import { ChatAppContext } from "@/Context/ChatApp/ChatAppStates";
import { useContext } from "react";

export default async function ChatAppChat() {
  // let { selectedChat } = useContext(ChatAppContext);

  return (
    <>
      <div>Chat Page</div>
    </>
  );
}
